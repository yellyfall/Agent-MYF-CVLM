{
  "name": "myf-candidature",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env --max-old-space-size=192 server.js",
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "docx": "^9.5.1",
    "esbuild": "^0.25.9",
    "mammoth": "^1.10.0",
    "pdfjs-dist": "^6.2.108"
  }
}
