{
  "name": "agent-ia-myf",
  "version": "3.0.0",
  "description": "Agent IA de MYF - Lettre de motivation + CV optimise",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env --max-old-space-size=192 server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "bcryptjs": "^3.0.2",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": "24.x"
  }
}